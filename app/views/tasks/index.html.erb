<h1>All Tasks</h1>
<ul>
  <%= "no tasks ;)" if @tasks.empty? %>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to(task.title, task_path(task)) %>--<%= link_to("Delete task", task_path(task), method:"delete", data: { confirm: "Are you sure?" }) %>
    </li>
  <% end %>
</ul>
